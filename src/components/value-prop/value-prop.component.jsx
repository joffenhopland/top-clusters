import {ValuePropContainer, ValuePropContent} from "./value-prop.styles"

const ValueProp = () => {
    return(
        <ValuePropContainer>
            value pro

        </ValuePropContainer>
    )
}

export default ValueProp